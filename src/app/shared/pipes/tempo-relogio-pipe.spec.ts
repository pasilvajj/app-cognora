import { TempoRelogioPipe } from './tempo-relogio-pipe';

describe('TempoRelogioPipe', () => {
  it('create an instance', () => {
    const pipe = new TempoRelogioPipe();
    expect(pipe).toBeTruthy();
  });
});
